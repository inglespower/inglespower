services:
  - type: web
    name: english-tutor-ia
    env: python
    pythonVersion: 3.10.12
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 10000
